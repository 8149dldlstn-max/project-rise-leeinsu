# 시작하기 (5분)

**준비물(최초 1회):** ① Claude Code 데스크톱 앱 — claude.ai/download 설치·로그인  ② Node.js LTS — nodejs.org

1. 이 폴더(`art-collab`)를 **바탕화면에 압축 풀기**
2. **Claude Code 데스크톱 앱** 열기 → `File > Open Folder` → 이 폴더 선택
   → 열면 `CLAUDE.md`가 **자동으로** 규칙·스펙을 물어옵니다 (내가 할 것 없음)
3. 셋업 PPT(`6주차_셋업_Supabase연결_따라하기`)를 보며 **Supabase 연결 → `.env` 만들기**
4. 첫 마디: **"CLAUDE.md와 docs를 읽었으면 요약해줘"** → 그다음 강사가 주는 프롬프트대로
5. 서버 켜기: 앱 **내장 터미널**에 `npm run start:dev` (또는 Claude에게 "실행해줘")
   → 브라우저 `http://localhost:3000/api-docs`

> 이 폴더 안:
> - `CLAUDE.md`  = Claude가 자동으로 읽는 규칙 (안 건드려도 됨)
> - `docs/규칙서.md` = 코드 작성 규칙(공통)
> - `docs/스펙.md`   = 내 앱의 설계도(엔티티·API·범위)

> 안 되면? `npm/node` 안 먹으면 → Node.js 설치 확인 · DB 연결 안 되면 → Supabase **Session pooler** 확인.
