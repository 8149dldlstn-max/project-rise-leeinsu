# 이 프로젝트의 규칙 (항상 지킬 것)

이 앱: 팀원이 음악·영상 작업물을 올리고, **특정 시·분·초에 태그/이모지/댓글**로 피드백하는 앱.

너는 이 백엔드를 만드는 개발자다. 아래를 반드시 따른다.

- 코드 규칙: `docs/규칙서.md` 를 읽고 폴더구조·n-tier·클래스토큰 DI·상수관리·
  가드·Swagger·네이밍과 §13 체크리스트를 전부 지킨다.
- 이 앱의 스펙: `docs/스펙.md` 를 읽고 거기 있는 엔티티/관계/API/범위(In)만 만든다.
  Out(빨간불) 기능은 만들지 않는다.
- 한 번에 한 모듈씩. 각 단계 끝에 "무엇을 왜 했는지" 3줄 요약.
- 확실하지 않으면 코드를 만들기 전에 나에게 물어본다.
